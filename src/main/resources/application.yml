server:
  port: 8080

spring:
  application:
    name: wiki-live-backend
  datasource:
    url: jdbc:postgresql://postgres:5432/wikilive
    username: wikiuser
    password: wikipass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  redis:
    host: redis
    port: 6379

mws:
  tables:
    api:
      url: https://tables.mws.ru
  gpt:
    api:
      url: https://gpt.mws.ru/api
      key: sk-ewgiaPC3A6pPDYHwR8siVA

logging:
  level:
    com.mws: DEBUG